<div class="page-width">
  <div class="grid">
    <div class="grid__item medium-up--one-whole">
      <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="rte">
        {{ page.content }}
        {% section 'Aboutus' %}
      </div>
    </div>
  </div>
</div>
